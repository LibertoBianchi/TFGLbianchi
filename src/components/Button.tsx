import React from 'react';
import PropTypes from 'prop-types';

Button.propTypes = {
    
};

function Button() {
    return (
        <div>
            <button></button>
        </div>
    );
}

export default Button;